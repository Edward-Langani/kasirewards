import {requireUser} from '@/lib/auth';import {db} from '@/lib/db';import {bad,ok} from '@/lib/api';export async function GET(){try{const u=await requireUser();const transactions=await db.transaction.findMany({where:{userId:u.id,status:'AVAILABLE'},orderBy:{createdAt:'desc'},take:100});return ok({balanceCents:transactions.reduce((s,t)=>s+t.amountCents,0),transactions})}catch{return bad('Unauthorized',401)}}
