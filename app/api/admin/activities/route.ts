import {NextRequest} from 'next/server';import {requireAdmin} from '@/lib/auth';import {db} from '@/lib/db';import {bad,ok} from '@/lib/api';
export async function GET(){try{await requireAdmin();return ok({activities:await db.activity.findMany({orderBy:{createdAt:'desc'}})})}catch{return bad('Forbidden',403)}}
export async function POST(req:NextRequest){try{await requireAdmin();const x=await req.json();const a=await db.activity.create({data:{title:String(x.title),description:String(x.description),category:String(x.category),rewardCents:Number(x.rewardCents),estimatedMinutes:Number(x.estimatedMinutes),provider:x.provider?String(x.provider):undefined,externalUrl:x.externalUrl?String(x.externalUrl):undefined}});return ok({activity:a},201)}catch{return bad('Could not create activity',400)}}
