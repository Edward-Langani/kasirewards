'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function Register(){const r=useRouter();const [f,setF]=useState({firstName:'',email:'',phone:'',password:'',province:'',referralCode:''});const [err,setErr]=useState('');const [busy,setBusy]=useState(false);async function submit(e:any){e.preventDefault();setBusy(true);setErr('');const res=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)});const j=await res.json();if(!res.ok){setErr(j.error||'Could not create account');setBusy(false);return}r.push('/dashboard');r.refresh()}return <main className="container"><form className="card form" onSubmit={submit}><span className="pill">Free account</span><h1>Join KasiRewards</h1><p className="muted">Start with the MVP and help us build a better rewards platform.</p><div className="stack"><div><label className="label">First name</label><input className="input" required value={f.firstName} onChange={e=>setF({...f,firstName:e.target.value})}/></div><div><label className="label">Email</label><input className="input" type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></div><div><label className="label">Mobile number</label><input className="input" value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/></div><div><label className="label">Province</label><select className="input" value={f.province} onChange={e=>setF({...f,province:e.target.value})}><option value="">Select province</option>{['Gauteng','Western Cape','KwaZulu-Natal','Eastern Cape','Free State','Limpopo','Mpumalanga','North West','Northern Cape'].map(x=><option key={x}>{x}</option>)}</select></div><div><label className="label">Password</label><input className="input" type="password" minLength={8} required value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></div><div><label className="label">Referral code (optional)</label><input className="input" value={f.referralCode} onChange={e=>setF({...f,referralCode:e.target.value})}/></div>{err&&<div className="error">{err}</div>}<button className="btn" disabled={busy}>{busy?'Creating…':'Create account'}</button><p className="small muted">By joining, you agree to the terms and privacy policy.</p></div></form></main>}
